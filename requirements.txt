# LearnTok AI — Python 依賴鎖定（Dependency Lock）
# 安裝方式見 scripts/setup.ps1
# 核心套件以 pyproject.toml 定義（pip install -e .）；本檔保留為 setup.ps1 的明細來源。

# === TTS 語音生成 ===
edge-tts==7.2.8

# === RVC 語音轉換 ===
# PyTorch（CUDA 12.1，需從 PyTorch 專用 index 安裝）
# torch==2.5.1+cu121
# torchaudio==2.5.1+cu121
rvc-python==0.1.5
faiss-cpu==1.14.3
librosa==0.11.0
pyworld==0.3.5
torchcrepe==0.0.24
praat-parselmouth==0.4.7
# fairseq（從本地 fairseq_build/ 安裝，見 setup.ps1）
# fairseq==0.12.2
hydra-core==1.3.4
omegaconf==2.3.1
loguru==0.7.3
python-multipart==0.0.32
uvicorn==0.51.0
ffmpeg-python==0.2.0
av==18.0.0
bitarray==3.9.2
sacrebleu==2.6.0

# === 其他（自動依賴，列出供參考）===
numpy==2.3.5
scipy==1.18.0
scikit-learn==1.9.0
numba==0.66.0
llvmlite==0.48.0
soundfile==0.14.0
resampy==0.4.3
audioread==3.1.0
tqdm==4.70.0
PyYAML==6.0.3
requests==2.34.2
# === RAG 知識庫（ChromaDB；階段二遷移 pgvector 見 README）===
chromadb==1.5.9
# 語意 Embedder（中英雙語，multilingual-e5-small）
sentence-transformers==5.6.1

# === LLM 腳本生成（script_gen.py）===
openai==2.52.0
python-dotenv==1.2.2
